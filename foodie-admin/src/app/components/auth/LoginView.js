import React from "react";
import { Button } from '@material-ui/core';

export class LoginView extends React.Component {
    render() {
        return (
            <div className={"container"}>
                <Button className={'login_btn'} href={''}>

                </Button>
            </div>
        );
    }
}